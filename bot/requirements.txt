# ── SIMP DATING BOT — Dependencies ──────────────────────────────
aiogram>=3.12.0
aiohttp>=3.10.0
asyncpg>=0.30.0
sqlalchemy[asyncio]>=2.0.0
redis[hiredis]>=5.0.0
aiohttp-socks>=0.9.0
boto3>=1.35.0
# AI-модерация (services/moderation.py). Без пакета текст падает на
# словарный фильтр, но фото-модерация fail-closed: КАЖДОЕ фото отклонялось
# бы даже с заданным ZHIPU_API_KEY — поэтому пакет обязателен, как в api.
zhipuai>=2.0.0
# zhipuai в рантайме делает `import sniffio`, но не декларирует его
# зависимостью, а свежие httpx/anyio больше не тянут sniffio транзитивно —
# без строки ниже чистая установка молча теряет клиент (ImportError глотается)
# и фото-модерация вечно fail-closed даже с ключом.
sniffio>=1.3.0
# Алертинг: включается только если задан SENTRY_DSN (см. services/alerting.py).
# Без DSN пакет даже не импортируется, поэтому его отсутствие не роняет бота —
# но в проде он должен быть установлен: упавший обработчик иначе виден только
# жалобой пользователя, логи Railway живут до следующего деплоя.
sentry-sdk>=2.0.0
