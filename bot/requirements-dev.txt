# ── SIMP DATING BOT — Dependencies для тестов ───────────────────
# Не ставится в прод: нужен только тем проверкам, которые прогоняют
# код бота на настоящей БД в памяти.
#
# У бота своя копия деки (database/connection.py), и ломается она ровно
# так же, как API-шная. Проверять её форму SQL недостаточно: мутация
# «блокировки только в одну сторону» сохраняет форму запроса и меняет
# смысл. Поэтому тесты поднимают SQLite в памяти и смотрят, кто реально
# вернулся из деки — см. api/tests/test_query_scale.py.
-r requirements.txt
aiosqlite>=0.20.0
