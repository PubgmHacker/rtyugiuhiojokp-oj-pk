# Публикация Souldawn в App Store

Пошаговая инструкция для категории Dating — самой строгой при ревью.
Проект собирается локально: `./tools/ios-build.sh simulator` уже проходит.

---

## 1. Что уже сделано в репозитории

| Пункт | Состояние | Где |
|---|---|---|
| Нативный iOS-проект (Capacitor 8, SPM) | готов | `web/ios/App` |
| Privacy Manifest | готов | `web/ios/App/App/PrivacyInfo.xcprivacy` |
| Описания разрешений (камера, фото, гео, микрофон) | готовы | `web/ios/App/App/Info.plist` |
| Иконка и splash в фирменных цветах | готовы | `web/ios/App/App/Assets.xcassets` |
| Портретная ориентация, тёмная тема | настроены | `Info.plist` |
| Удаление аккаунта внутри приложения | готово | `web/src/pages/Profile.tsx` |
| Жалоба и блокировка | готовы | экран чата и профиля |
| Юридические страницы | готовы | `landing/*.html` |
| Скрипт сборки и архивации | готов | `tools/ios-build.sh` |
| Deployment target | iOS 16.0 | `project.pbxproj` |

---

## 2. Подготовка в Apple Developer Portal

1. **Аккаунт.** Нужен платный Apple Developer Program (99 $/год). Для
   dating-приложения выгоднее оформить на организацию: приложения от
   Individual чаще получают дополнительные вопросы при ревью.
2. **Bundle ID.** Зарегистрируйте `com.souldawn.dating` (Identifiers →
   App IDs). Включите Capability **Push Notifications**.
3. **APNs-ключ.** Keys → создайте ключ с Apple Push Notifications service,
   скачайте `.p8` (скачать можно один раз). Сохраните Key ID и Team ID —
   они понадобятся бэкенду для отправки пушей.
4. **Team ID.** Membership → Team ID. Он нужен скрипту архивации.

---

## 3. Сборка и загрузка

```bash
# Проверка, что всё собирается (без подписи)
./tools/ios-build.sh simulator

# Архив для App Store (нужна настроенная подпись)
DEVELOPMENT_TEAM=XXXXXXXXXX ./tools/ios-build.sh archive
```

Скрипт сам соберёт веб-бандл, синхронизирует его с нативным проектом,
создаст `.xcarchive` и экспортирует `.ipa` в `build/ios/`.

Загрузка в App Store Connect:

```bash
xcrun altool --upload-app -f build/ios/App.ipa -t ios \
  --apiKey <KEY_ID> --apiIssuer <ISSUER_ID>
```

Ключ App Store Connect API создаётся в App Store Connect → Users and
Access → Integrations. Альтернатива — приложение Transporter или
Xcode → Product → Archive → Distribute App.

Перед каждой загрузкой поднимайте `CURRENT_PROJECT_VERSION` (build
number): App Store Connect не принимает повторную загрузку с тем же
номером.

---

## 4. Метаданные в App Store Connect

**Возрастной рейтинг: 17+.** Обязательно отметьте в анкете рейтинга
«Frequent/Intense Sexual Content or Nudity» → *None*, но категорию
Dating указать необходимо; приложения знакомств ниже 17+ не проходят.

**Категория:** Primary — Social Networking, Secondary — Lifestyle.

**Обязательные URL** (разверните `landing/` и подставьте реальный домен):
- Privacy Policy URL → `https://souldawn.app/privacy.html`
- Terms of Use (EULA) → `https://souldawn.app/terms.html`
- Support URL → `https://souldawn.app/support.html`
- Marketing URL → `https://souldawn.app/`

**App Privacy.** Заполните Nutrition Labels строго в соответствии с
`PrivacyInfo.xcprivacy`: имя, фото, приблизительное местоположение,
идентификатор пользователя, пользовательский контент — всё «связано с
пользователем», отслеживание отсутствует.

**Скриншоты.** Обязательны для 6.9″ (iPhone 17 Pro Max, 1320×2868) и
6.5″ (1284×2778). Нужно 3–10 штук: дека, экран мэтча, чат, профиль,
настройки. Проще всего снять на симуляторе:

```bash
xcrun simctl boot "iPhone 17 Pro Max"
xcrun simctl io booted screenshot shot1.png
```

Скриншоты не должны содержать реальных лиц без прав на использование и
не должны обещать функции, которых нет.

---

## 5. Что чаще всего приводит к отклонению

**Guideline 1.2 — User Generated Content.** Для приложений с UGC нужны
все четыре механизма, и ревьюер их проверяет вручную:
- фильтрация недопустимого контента (у нас — AI-модерация фото и текста);
- механизм жалобы на контент;
- блокировка пользователя;
- контактные данные для обращений.

Все четыре реализованы, но убедитесь, что жалоба и блокировка доступны
не более чем в два тапа из чата и из профиля, и что модерация реально
работает на продакшене (задан `ZHIPU_API_KEY`, иначе останется только
фильтр по ключевым словам).

**Guideline 3.1.1 — In-App Purchase.** Это главный риск проекта.
Сейчас Premium продаётся через Telegram Stars и CryptoBot. Apple
требует, чтобы разблокировка функций **внутри iOS-приложения**
продавалась только через IAP, и запрещает вести пользователя к внешней
оплате. Варианты:
1. Добавить настоящий StoreKit IAP для покупки Premium в iOS-сборке
   (рекомендуемый путь, но требует нативной работы и серверной валидации
   чеков).
2. В iOS-сборке полностью скрыть любые упоминания Premium, покупки и
   ссылки на бота, оставив монетизацию только в Telegram и в вебе
   («reader»-подобная модель). Экран профиля должен определять
   `isNative()` и не показывать блок Premium.

Второй вариант быстрее и безопаснее для первого релиза. Текущий код
показывает блок Premium всегда — **перед отправкой это нужно закрыть
условием**, иначе отклонение почти гарантировано.

**Guideline 4.8 — Sign in with Apple.** Требуется, если приложение
использует сторонний сервис входа (Telegram). Формально требование
касается «third-party login services» вроде Google/Facebook; вход через
Telegram ревьюеры трактуют по-разному. Безопасный путь — добавить Sign
in with Apple как второй способ входа. Если этого нет, будьте готовы
обосновать в Review Notes, что Telegram — не социальный логин-провайдер
в смысле 4.8, а транспорт самого сервиса.

**Guideline 5.1.1(v) — удаление аккаунта.** Реализовано: Профиль →
Удалить аккаунт, с подтверждением. Проверьте, что серверный
`DELETE /profiles/me` действительно удаляет данные, а не помечает флагом.

**Демо-доступ.** Ревьюеру нужен рабочий вход без Telegram. Подготовьте
тестовый аккаунт и укажите логин в App Review Information, либо
опишите, как войти. Пустая дека при первом входе выглядит как
неработающее приложение — заполните базу тестовыми анкетами.

---

## 6. Перед отправкой — чек-лист

- [ ] `./tools/ios-build.sh simulator` проходит
- [ ] Блок Premium скрыт в нативной сборке **или** реализован StoreKit IAP
- [ ] Sign in with Apple добавлен или подготовлено обоснование
- [ ] Домен `souldawn.app` работает, все юридические страницы открываются
- [ ] `VITE_API_URL` в сборке указывает на продакшен, не на localhost
- [ ] `DEBUG=false` на бэкенде: `/docs` и гостевой вход недоступны
- [ ] `ZHIPU_API_KEY` и ключи R2 заданы — модерация и фото работают
- [ ] В базе есть тестовые анкеты, чтобы дека не была пустой
- [ ] Тестовый аккаунт для ревьюера создан и указан в Review Notes
- [ ] Скриншоты 6.9″ и 6.5″ загружены
- [ ] Возрастной рейтинг 17+ выставлен
- [ ] Build number увеличен
- [ ] TestFlight: прогон на реальном устройстве перед публикацией

---

## 7. Пуш-уведомления

Клиентская часть готова (`web/src/lib/native.ts`:
`registerPushNotifications`). Осталась серверная:

1. Эндпоинт `POST /profiles/me/devices` для приёма APNs-токена
   (клиентский вызов `registerDevice` уже есть в `web/src/lib/api.ts`).
2. Отправка через APNs по `.p8`-ключу при новом мэтче или сообщении.
3. Сброс бейджа — `clearNotificationBadge()` при открытии списка чатов.

Без серверной части приложение работает нормально, просто уведомления
приходят только внутри Telegram.
