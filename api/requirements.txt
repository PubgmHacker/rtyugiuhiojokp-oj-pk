# ── SIMP DATING API — Dependencies ──────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
redis[hiredis]>=5.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.12
boto3>=1.35.0
zhipuai>=2.0.0
# zhipuai в рантайме делает `import sniffio` (core/_utils/_utils.py), но не
# декларирует его зависимостью, а свежие httpx (0.28+) и anyio (4.14+) больше
# не тянут sniffio транзитивно. Без строки ниже чистая установка молча теряет
# Zhipu-клиент (ImportError глотается фабрикой) — фото-модерация вечно
# fail-closed, ИИ-мэтчер выключен. Ловится tests/test_zhipu_endpoint.py.
sniffio>=1.3.0
aiohttp>=3.10.0
Pillow>=11.0.0
alembic>=1.13.0
# APNs требует HTTP/2 — h2 подтягивается экстрой httpx[http2]
httpx[http2]>=0.27.0
# Проверка подписи покупок App Store: собирает цепочку сертификатов до
# Apple Root CA G3 и учитывает случаи, когда Apple подписывает валидные
# транзакции формально истёкшим промежуточным сертификатом
app-store-server-library>=1.5.0
# Алертинг: включается только если задан SENTRY_DSN (см. services/alerting.py).
# Без DSN пакет даже не импортируется, поэтому его отсутствие не роняет
# приложение — но для реальных проектов он должен быть установлен.
sentry-sdk>=2.0.0

# ── Тесты (нужны только в разработке и CI) ──────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
# Стоп-кран на зависший тест: в CI один повисший await съедал весь лимит
# джобы (30 минут) и снимал её без единой строки о виновнике. Метод thread
# дампит стеки всех потоков — видно и повисший event loop, и поток TestClient
pytest-timeout>=2.4.0
# Настоящая БД для сквозных тестов: подменённая сессия проверяет ответы, но не
# ловит того, что видно только на живой базе — так нашлись UnboundLocalError на
# пустой деке и падение сравнения naive/aware времени
aiosqlite>=0.20.0
httpx>=0.27.0
