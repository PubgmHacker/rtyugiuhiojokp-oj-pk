[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "postgresql_16"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT --workers ${WEB_CONCURRENCY:-2}"
