"""Скрипты, которые прогоняются интерпретатором БОТА, а не API.

Бот живёт в отдельном venv (Python 3.12 против 3.14 у API) и импортировать
его код из тестов API нечем. Поэтому проверки его поведения оформлены
обычными скриптами: тест запускает их через `subprocess` интерпретатором
бота и читает JSON с последней строки stdout.

Отдельными файлами, а не строками в тесте: скрипт на 50 строк внутри
тройных кавычек не подсвечивается, не проверяется линтером и правится
вслепую.
"""
